# Gemfile for AtlasTracker
# Run `bundle install` to install dependencies

source "https://rubygems.org"

# Fastlane for build automation
gem "fastlane", "~> 2.219"

# Code quality plugins (optional)
gem "fastlane-plugin-versioning", "~> 0.5"
